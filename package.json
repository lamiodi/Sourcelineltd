{
  "name": "sourceline-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "workspaces": [
    "client",
    "server"
  ]
}